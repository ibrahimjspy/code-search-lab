# Synthetic sample project

These small, original examples exercise cache expiry, configuration validation,
queue operations and a Go text-window fallback. They have no external services,
business identifiers, credentials or production data. They are retrieval fixtures,
not production implementations or a representative search-quality benchmark.
