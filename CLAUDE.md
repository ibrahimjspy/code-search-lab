# Code Search Lab

Read and follow `AGENTS.md`. This tool works through a Python CLI and requires no
MCP server. Select a source directory explicitly with `--repo` when the current
directory is not the intended target.
